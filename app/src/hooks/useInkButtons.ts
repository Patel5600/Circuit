/**
 * useInkButtons — Static & performant.
 * All pointer tracking, MutationObservers, and magnetic RAF loops are removed
 * to guarantee 60fps smooth scrolling with zero CPU overhead and no freezing.
 */
export function useInkButtons() {
  // Static hook — tracking completely removed as requested
}
